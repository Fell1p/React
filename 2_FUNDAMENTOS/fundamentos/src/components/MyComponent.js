// Este componente será reindenizado em "FistComponent.JS" e reaproveitado em "App.JS", assim o caracterizando como componente filho e irmão ao mesmo tempo, isso se chama reciclagem de componentes.

const MyComponent = () => {
    return (
        <div>
            <h2>Estou sendo Reaproveitado em vários lugares!</h2>
        </div>
    );
};

export default MyComponent;